* text=auto

# Normalize line endings for scripts
*.sh eol=lf
*.ps1 eol=crlf
*.bat eol=crlf

# Python files use LF (PEP 8 standard)
*.py eol=lf

# Markdown and text files use LF
*.md eol=lf
*.txt eol=lf

# Prefer C/C++-aware diffs
*.c  diff=cpp
*.cc diff=cpp
*.cpp diff=cpp
*.cxx diff=cpp
*.h  diff=cpp
*.hh diff=cpp
*.hpp diff=cpp

# Binary files (no diffs)
*.png binary
*.ico binary
*.exe binary
*.dll binary
*.lib binary
*.pdb binary

# Git LFS for large archives (track backups and large blobs)
*.zip filter=lfs diff=lfs merge=lfs -text
*.tar.gz filter=lfs diff=lfs merge=lfs -text
*.tgz filter=lfs diff=lfs merge=lfs -text

# Additional LFS tracking for diagnostics and installers
*.dmp filter=lfs diff=lfs merge=lfs -text
*.msixbundle filter=lfs diff=lfs merge=lfs -text
*.msix filter=lfs diff=lfs merge=lfs -text
*.appx filter=lfs diff=lfs merge=lfs -text
changes/security_reports/** filter=lfs diff=lfs merge=lfs -text
